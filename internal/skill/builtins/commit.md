---
description: create a git commit for the current changes
---
Create a git commit for the current changes:
1. Run `git status` and `git diff` (and `git diff --staged`) to see what changed.
2. Stage the relevant files.
3. Write a concise, imperative commit subject and a body explaining the why.
4. Create the commit. Do not push unless asked.
