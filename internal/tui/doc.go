// Package tui will contain the Bubble Tea interactive REPL (Phase 4).
// The engine emits events on a channel; the TUI consumes them and renders the
// transcript, streaming output, prompt input, and permission dialogs.
// No business logic belongs here.
package tui
