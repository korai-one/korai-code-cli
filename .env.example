# Copy this file to .env and fill in a key. The .env file is gitignored.
# Real values exported in the environment always override this file.
#
# Backend selection: if KORAI_API_KEY is set, the CLI uses the Korai P2P
# inference network; otherwise it falls back to ANTHROPIC_API_KEY. Set only one.

# --- Korai backend (preferred) ---
KORAI_API_KEY=sk-korai-...
# Optional: override the orchestrator URL (defaults to https://cloud.korai.one).
# KORAI_BASE_URL=http://localhost:8080

# --- Anthropic backend (migration fallback) ---
# ANTHROPIC_API_KEY=sk-ant-...
